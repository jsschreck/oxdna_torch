name: oxdna_torch
channels:
  - conda-forge
  - pytorch
dependencies:
  - python=3.10
  - numpy
  - pytorch
  - pytest
  - flake8
  - pip:
    - oxDNA-analysis-tools
    - -e .
    # Optional: TorchMD-Net neighbour-list backend (faster GPU NL kernel)
    # Uncomment to install:
    # - torchmd-net